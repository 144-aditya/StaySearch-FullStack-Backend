# PostgreSQL Configuration
PGHOST=localhost
PGPORT=5432
PGDATABASE=staysearch_db
PGUSER=postgres
PGPASSWORD=postgres

# Server Configuration
PORT=8080

# For Railway Deployment (use these names)
# PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD